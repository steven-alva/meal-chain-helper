# 接龙点餐小助手交付清单

建议把整个 `meal-chain-helper` 文件夹交给主管。

主管需要看到的文件：

- `启动接龙点餐小助手.command`：双击启动。
- `主管使用说明.md`：简短使用说明。
- `data/menu_templates.yml`：菜单模板文件，页面会自动读取和保存。

不要删除的文件夹：

- `app/`
- `meal_helper/`
- `data/`
- `.streamlit/`

如果要移动位置，直接移动整个 `meal-chain-helper` 文件夹即可。

